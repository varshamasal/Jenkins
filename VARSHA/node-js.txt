pipeline {
    agent {
        label 'rani'
    }
    stages {
        stage('Pull a file from git') {
            steps {
                git 'https://github.com/Mayur2905/TicTacToe.git'
            }
        }
        stage('Building the app') {
            steps {
                sh 'sudo apt install nodejs --yes' // corrected installation command for nodejs
                sh 'sudo apt install npm --yes' // corrected installation command for npm
                sh 'npm install'
            }
        }
        stage('Running the app') {
            steps {
                sh 'npm run start & wait' // corrected command to start the app
            }
        }
    }
}
